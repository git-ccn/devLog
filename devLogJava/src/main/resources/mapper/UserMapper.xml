<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.devlogjava.mapper.UserMapper">

    <select id="findByPhone" resultType="UserPo">
        SELECT id, phone, password, nickname, create_time, update_time
        FROM sys_user
        WHERE phone = #{phone}
    </select>

    <insert id="insert" parameterType="UserPo">
        INSERT INTO sys_user (id, phone, password, nickname)
        VALUES (#{id}, #{phone}, #{password}, #{nickname})
    </insert>

    <update id="updatePasswordById">
        UPDATE sys_user
        SET password = #{password}
        WHERE id = #{id}
    </update>

</mapper>
