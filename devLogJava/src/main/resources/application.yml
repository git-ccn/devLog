spring:
  profiles:
    active: pro
  application:
    name: devLogJava
  datasource:
    url: jdbc:mysql://localhost:3306/devlog?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=GMT%2B8&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  data:
    redis:
      host: localhost
      port: 6379
      password: 
      database: 0

mybatis-plus:
  mapper-locations: classpath*:mapper/**/*.xml
  type-aliases-package: com.example.devlogjava.entity
  configuration:
    map-underscore-to-camel-case: true
# JWT 配置
jwt:
  secret: devlog_secret_key_2026_future_study_learning_log_system
  expiration: 604800 # 7天，单位秒
# DeepSeek 配置
deepseek:
  api-key: ${DEEPSEEK_API_KEY:}
  base-url: https://api.deepseek.com
  model-name: deepseek-v4-flash
server:
  port: 8082

